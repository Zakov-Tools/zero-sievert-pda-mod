if (!instance_exists(polt_id))
    instance_destroy()
else if (polt_id.state == scr_enemy_patrol)
{
    var quanti = polt_id.weapon_number_max
    var angolo = ((360 / quanti) * my_number)
    for (var i = 0; i < quanti; i++)
    {
        if (i == my_number)
        {
        }
    }
}
